pipeline {
    agent any

    stages {
        stage('VM node version') {
            steps {
                echo 'VM node version'
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}